# Configuration reference

The complete policy map, defaults, file boundaries, metrics limits and troubleshooting are in [OPERATIONS](OPERATIONS.md). The installable default is [config.yml](../agent/src/main/resources/config.yml); the host example is [host-config.json](../host-agent/examples/host-config.json). Local files are authoritative and browser roles cannot override them.
