# Historical rc.2 mock gateway

The v2 simulator is retired; its entry point fails explicitly. Protocol 3 tests live in the dashboard repository: `npm run relay:test` and `npm run relay:smoke`. No production credentials are needed.
